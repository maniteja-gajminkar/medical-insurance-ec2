name: Deploy and Train

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-and-train:
    runs-on: ubuntu-latest

    steps:
    - name: 🧾 Checkout repository
      uses: actions/checkout@v3

    - name: 🔐 Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: 🚀 Start MLflow server on EC2
      run: |
        aws ssm send-command \
          --document-name "AWS-RunShellScript" \
          --instance-ids i-0ebb50246a4b9c539 \
          --comment "Start MLflow server" \
          --parameters 'commands=[
            "source /home/ec2-user/mlflow-venv/bin/activate",
            "nohup /home/ec2-user/mlflow-venv/bin/mlflow server \
              --backend-store-uri sqlite:///home/ec2-user/mlflow.db \
              --default-artifact-root s3://mlflow-artifacts-maniteja \
              --host 0.0.0.0 \
              --port 5000 > /home/ec2-user/mlflow.log 2>&1 &"
          ]' \
          --region ${{ secrets.AWS_REGION }}

    - name: 🔍 Check MLflow API health
      run: |
        curl --max-time 10 -sSf "http://35.171.186.148:5000/api/2.0/mlflow/experiments/list" || echo "⚠️ MLflow API not responding"

    - name: 🧪 Trigger training on EC2 via SSM
      run: |
        aws ssm send-command \
          --document-name "AWS-RunShellScript" \
          --instance-ids i-0ebb50246a4b9c539 \
          --comment "Run training script" \
          --parameters 'commands=[
            "source /home/ec2-user/mlflow-venv/bin/activate",
            "pip install -r /home/ec2-user/medical-insurance-ec2/requirements.txt",
            "export MLFLOW_TRACKING_URI=http://127.0.0.1:5000",
            "cd /home/ec2-user/medical-insurance-ec2",
            "python3 train.py"
          ]' \
          --region ${{ secrets.AWS_REGION }}